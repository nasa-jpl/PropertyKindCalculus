/-
# Validation probes — the Graph library (the incidence-quiver bridge)

Inhabitation, executable-instance, and axiom-profile probes for the `Graph` library: the
flow-digraph theorems applied at the core influence probes' concrete graph, the kind-level
component computation on a hand-built graph, and the `#kind_scc` report and
`#kind_scc_clean` gate driven at a crafted laundering loop. See `Tests.Graph.Bridge`.
-/

module

public import PropertyKindCalculus.Tests.Graph.Bridge

@[expose] public section Blanket



end Blanket
