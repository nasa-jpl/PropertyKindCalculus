/-
# ForPhysLib.Examination — the physics that individuates the kinds

The mirror of `ForPhysLib.Kinds`, file for file: each module declares the examination
principles (Dybkær §7.5) its kinds are individuated by, proves the `examinedBy` links, and
derives distinctness from them. See [PLAN.md](ForPhysLib/PLAN.md#stage-0-the-kind-vocabulary).

  * `Space` — the principles of `Physlib/SpaceAndTime/Space`'s vocabulary.
-/

module

public import ForPhysLib.Examination.Space

@[expose] public section



