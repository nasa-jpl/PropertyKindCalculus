/-
# Stage 4 aggregator — the audits, per adopted directory

One module per adopted PhysLib directory: the pinned boundary audit, the silent
clean/ratchet gates, and the unkinded ledger over declared contracts — the CI face of
the adoption, beside the `checked_by:` delta proposed for the directory's
`API-map.yaml`. See `PLAN.md`, [Stage 4](PLAN.md#stage-4-audits-and-the-api-map).
-/

module

public import ForPhysLib.Audits.Space

@[expose] public section



