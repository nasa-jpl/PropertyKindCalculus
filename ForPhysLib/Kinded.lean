/-
# Stage 2 aggregator — the kinded author-forms, per adopted directory

One module per adopted PhysLib directory: the directory's quantities re-authored kinded,
with the naked form `rfl`-equal to `.magnitude` of the kinded one, so existing theorem
statements survive by definitional erasure. See
`PLAN.md`, [Stage 2](PLAN.md#stage-2-kinded-re-authoring-with-definitional-erasure).
-/

module

public import ForPhysLib.Kinded.Space

public section -- pkc-blanket
@[expose] section -- pkc-blanket-expose



end -- pkc-blanket-expose
end -- pkc-blanket
