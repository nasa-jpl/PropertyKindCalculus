/-
# Exhibit G — Composition

The exhibit occasioned by [MARMODORO-CORPUS.md](../MARMODORO-CORPUS.md): where a rigid body
is a *many* treated as a *one*, which of its quantities come from its parts, and where the
library says so. `Findings.lean` holds the build artifacts — four inhabitants of
`InformalLemma`, a massless body with a centre of mass, three sums the types cannot tell
apart, and the kinded counter-form that splits them three ways.
-/

module

public import ForPhysLib.Exhibits.Composition.Findings

public section -- pkc-blanket
@[expose] section -- pkc-blanket-expose



end -- pkc-blanket-expose
end -- pkc-blanket
