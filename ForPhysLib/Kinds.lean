/-
# ForPhysLib.Kinds — Stage 0, the kind vocabulary

One module per PhysLib directory: bare `KindOfProperty` declarations, Mathlib-free,
looked up from ISO 80000 (via PKC's `Iso80000` catalogue — agreement proved in Stage 1)
rather than designed. See [PLAN.md](ForPhysLib/PLAN.md#stage-0-the-kind-vocabulary).

  * `Space` — the vocabulary of `Physlib/SpaceAndTime/Space`, the plan's first directory.
-/
import ForPhysLib.Kinds.Space
