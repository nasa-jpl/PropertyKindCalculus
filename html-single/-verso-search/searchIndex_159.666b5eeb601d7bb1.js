window.docContents[159].resolve({"/#A-Property-Kind-Calculus-for-Metrology___-proving-quantity-and-unit-laws-in-Lean___-from-ISO___IEC-80000-to-measurement-uncertainty--ISO-80000-3-___-Space-and-Time--Dimension-does-not-classify_SEMI_-the-kind-does":{"contents":"Part 3 alone is dense with dimension collisions — distinct kinds that the dimension\nfunctor cannot tell apart. Plane angle, rotational displacement, phase angle, solid\nangle, rotation, and the logarithmic decrement are all dimension one. Frequency,\nrotational frequency, angular frequency, angular velocity, and the damping coefficient\nare all T⁻¹. Velocity and speed are both L·T⁻¹. The dimension is the same; the\nkind is not — and so the units differ too, a radian is not a steradian and a hertz is\nnot a radian-per-second, though each pair shares a dimension.\n\n\n\nThe dimension-1 disambiguation, on the standard. There exist distinct ISO 80000-3\nkinds with the same dimension one — plane angle and solid angle witness it. No\ndimension-only type system can separate them; the kind layer does. This generalizes\nthe calculus's dim_not_injective capstone to the published standard. Uses\nthe dimension map.\n\niso80000_3_dim_one_collision : ∃ a b : DimensionedKind, a.kind ≠ b.kind ∧ a.dim =\nb.dim ∧ a.dim = 1, witnessed by ⟨planeAngle, solidAngle, …⟩ with\nplaneAngle_ne_solidAngle (a decide) and reflexivity. Axiom-free.\n\nA radian is not a steradian, though both are dimension one. The SI units of plane and\nsolid angle are not commensurable, because their kinds differ — a type-level fact the\ndimension cannot see. This is exactly the separation the angle-reform literature seeks to\ninstall in the dimension layer — Leonard\n\nwould give angle its own dimension and make solid angle its square. PKC secures it at the\nkind layer — so it holds whether or not the SI ever assigns angle a dimension — and,\nhaving made PhysLib's Dimension parametric in its basis, now realizes the reform itself\nin an angle-augmented basis where plane angle is a base dimension and solid angle its\nsquare.\nThe hertz and the radian-per-second are separated the same way (both T⁻¹). Uses\nmetrological units.\n\nradian_steradian_not_commensurable : ¬ radian.Commensurable steradian by unfolding to\nthe underlying kinds and decide; hertz_radianPerSecond_not_commensurable is the\nT⁻¹ companion.\n\nDimension is not a classifier even away from dimension one. Distinct ISO 80000-3 kinds\nalso share dimensionful dimensions — frequency and angular frequency are both T⁻¹,\nvelocity and speed both L·T⁻¹. Uses the dimension map.\n\niso80000_3_dim_collision, witnessed by ⟨frequency, angularFrequency, …⟩ with\nfrequency_ne_angularFrequency (a decide) and reflexivity; velocity_ne_speed is the\nL·T⁻¹ companion. Axiom-free.\n\n","context":"A Property Kind Calculus for Metrology: proving quantity and unit laws in Lean, from ISO/IEC 80000 to measurement uncertainty\u0009ISO 80000-3 — Space and Time","header":"19.3. Dimension does not classify; the kind does","id":"/#A-Property-Kind-Calculus-for-Metrology___-proving-quantity-and-unit-laws-in-Lean___-from-ISO___IEC-80000-to-measurement-uncertainty--ISO-80000-3-___-Space-and-Time--Dimension-does-not-classify_SEMI_-the-kind-does"},"/#A-Property-Kind-Calculus-for-Metrology___-proving-quantity-and-unit-laws-in-Lean___-from-ISO___IEC-80000-to-measurement-uncertainty--Write-Once___-Correctly--Go-fast___-automatically-___-the-deployment-spectrum--Right-sizing-the-deployment-___-the-memory-shape-is-computed___-the-platform-is-asked":{"contents":"Choosing a strategy is only half of a deployment decision; the other half is a number — how many\ntask-shards the multi-threaded strategy forks, or how many pixels a device batch carries. Both are\nbounded by memory before they are bounded by compute: the eager device carrier materializes a full\nbuffer per live intermediate, and the sharded CPU path holds every concurrent slice at once. And the\nbound is a property of this model on this machine, so a hand-set constant is wrong somewhere —\ncores idle on the large host, or an out-of-memory kill on the confined worker. The calculus\ntherefore treats sizing the way it treats everything else in this chapter: the model's memory shape\nis computed, from the same recording that already proves faithfulness and computes arithmetic\nintensity; the platform is asked what it will actually grant; and a small solver combines the two —\nwith an explicit operator override that wins verbatim when someone knows better.\n\nThe model half is computed, not cited — the same standard the intensity accounting set. Memory use\nis affine in the pixel count for every strategy, bytes(P) = fixed + perElement · P, and the\nper-element term falls out of the recorded DAG per strategy. For the fused megakernel it is exact:\ninputs plus outputs, four bytes each — every intermediate is a register — with a bound table charged\nonce per launch. For the eager carrier it is the peak count of simultaneously-live buffers when\nthe DAG is evaluated in tape order, each buffer freed after its last consumer: one pass recovers\nevery node's last use, one walk takes the running peak (maxLiveNodes, reported beside the\nintensity numbers by the same aiReport), and the sharded CPU path adds its host marshaling columns\non top. One honesty clause belongs here: the eager number is a lower bound, because \"freed\npromptly after its last consumer\" is what a garbage-collected finalizer approaches rather than\nguarantees, and a driver that loops a recorded step holds its carry across iterations. So the static\nshape is the prior — what makes a freshly authored model safely sizable before it has ever run —\nand a measured probe of a real block (allocator peak on the device, child peak memory on the host)\nis the authority that calibrates it.\n\nThe platform half is asked with its provenance kept, because the budget that matters is the one\nenforcement reads. A confined process is killed at its cgroup limit, not at the host's advertised\navailable memory — a container capped at 28 GiB on a host reporting 62 GiB is the observed norm on\nshared worker queues — so the budget query asks cgroup v2 first (the process's own cgroup, minimum\nover its ancestors), falls back to cgroup v1, and only then to the host's own figure, every answer\nlabeled with its source so the log records which limit the sizing believed. Core counts get the\nsame discipline: the scheduler's affinity mask intersected with the cgroup CPU quota, never the raw\nhost core count. On the device the query is the CUDA free-memory figure, taken after the carrier's\nwarm-up — the context's fixed tax is then already netted out of it, which is one fixed overhead\nnobody has to specify.\n\nTwo solver shapes close the loop, because the two knobs constrain differently. Task-sharding\npartitions one resident tile among concurrent shards, so the variable term is invariant in the\nshard count and only a per-shard fixed cost scales with it — and when the resident term alone\nexceeds the budget, no shard count fits, and the solver says exactly that (shrink the tile, not the\ncount) instead of clamping to a plausible-looking answer. Block-concurrency — a fixed block size,\nchoosing how many run at once — divides the remaining headroom by the per-worker cost. Either way\nthe decision is announced on one line with every term and its source, so an out-of-memory kill or an\ninexplicably idle machine is diagnosable from the log alone; and the validation suite pins the\npeak-residency walk on hand-computed live-set structures and the capacity parsers on captured\nplatform records, so the shapes are checked facts under the same CI as the parity theorems.\n\n","context":"A Property Kind Calculus for Metrology: proving quantity and unit laws in Lean, from ISO/IEC 80000 to measurement uncertainty\u0009Write Once, Correctly\u0009Go fast, automatically — the deployment spectrum","header":"15.3.8. Right-sizing the deployment — the memory shape is computed, the platform is asked","id":"/#A-Property-Kind-Calculus-for-Metrology___-proving-quantity-and-unit-laws-in-Lean___-from-ISO___IEC-80000-to-measurement-uncertainty--Write-Once___-Correctly--Go-fast___-automatically-___-the-deployment-spectrum--Right-sizing-the-deployment-___-the-memory-shape-is-computed___-the-platform-is-asked"}});