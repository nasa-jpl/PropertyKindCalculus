/-
# KindCalculus.Examples

Aggregator for the worked examples. These live in a **separate library**
(`Examples`, source tree `examples/`) so that downstream users can depend on the
core `KindCalculus` library without pulling in any example code, or import the
examples explicitly via `import KindCalculus.Examples`.
-/

import KindCalculus.Examples.MiniLengthWidth
