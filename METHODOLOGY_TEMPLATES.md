# METHODOLOGY_TEMPLATES.md — closing the application templates: from exemplars to censuses

> Status: **steps 1 and 2 landed; step 3 landed for M12, M15 and M21's domain half; step 4
> landed for M20 and M21's ambiguity half (`Relation.wellPosed`/`domain`/`ambiguity`,
> censused by `#kind_wellposedness_coverage`) and for M22 (`@[kindDiagnostic]`, censused by
> `#kind_diagnostic_coverage`); M6, M12, M15, M20, M21 and M22 now read `gate` in the
> catalogue. M17 (and M18, M19, M26, D6–D8, D13) still needs its declaration form
> first.** Steps 4 (rest) and 5 open.
> Baseline 2026-09-07.
> Audience: PKC maintainers, and the author of any document that scores itself against
> the model or deployment template.
> Scope: make a rubric's *green* mean "the machine walked the population", wherever a
> population exists to walk — and say plainly, in the catalogue, where none does.

---

## 1. The finding this plan answers

Both application templates — the model template
(`PropertyKindCalculus.Rubrics.modelTemplate`, argued in the blueprint chapter *Applying the
calculus to a domain*) and the deployment template (`deploymentTemplate`, *Deploying a
domain model*) — are discharged by **sites**: a document names
one declaration and one section per rubric, and `Conformance.status` reports the rubric's
`dischargedWord` once each channel its evidence kind requires has a site. A site is one
thing. It is never a claim about the rest.

Read against soil-moisture-model's technical reference, that produced a concrete gap
(2026-09-07):

* M6 — *kinds of dimension one are individuated by their examination principle* — is
  scored `generated` on the strength of two annotated theorems in
  `algorithm/dielectric/examination/coherence.lean`. A probe over the same module set the
  conformance report scores found **100 `KindOfProperty` constants under `SoilMoisture`,
  75 with `examPrinciple = none`** — among them the four dimension-one kinds the model
  proves are PKC's own motivating example (volumetric and gravimetric water content,
  relative permittivity, reflectivity), whose distinctness rests on `by decide` over their
  `id` strings. A third principle-separated theorem in the same file
  (`transitionMoisture_ne_relPerm`) carries no annotation at all.
* The mechanism cannot see any of this. `Conformance.status` tests whether the site list
  is non-empty; two theorems and two hundred read identically.

The general form: **a rubric discharged by exemplars is only as complete as the author's
memory.** The rest of this document is about which rubrics can be discharged by anything
else, and how.

## 2. The framing (fixed in the catalogue; argued in the blueprint)

Each rubric now carries a `closure` (`PropertyKindCalculus.Rubrics.Closure`), orthogonal to
its evidence kind:

| closure | meaning |
|---|---|
| `prose` | the population is what the document *says*; no census is possible, by construction |
| `pending population` | the population can be enumerated from the environment; nothing walks it yet |
| `record mechanism` | a generated view or a `#guard_msgs`-pinned reading — cannot omit a member; a changed answer is a visible diff |
| `gate mechanism` | an audit that fails the build on a member that does not satisfy the rubric |

A census needs three things, and each either exists or does not: **a population that is a
type** (declaring a member enrolls it — `#kind_contracts` is the model); **a decidable
per-member predicate**; **declarable exceptions** (`@[kindCounterexample]`, the declared
clusters of `#kind_scc_clean`). The full argument is the chapter section *What the machine
can hold, and what it cannot*; this file does not repeat it.

Profile at baseline, generated by `rubric_closure_count` (not typed here — re-run it):
model template 26 = 5 prose + 10 held (4 gated) + 11 pending; deployment template
17 = 7 prose + 5 held (2 gated) + 5 pending.

## 3. The plan

The plan covers both templates. The eleven pending model rubrics and the five pending
deployment rubrics are not one kind of work.

| bucket | rubrics | what the work actually is |
|---|---|---|
| population exists — write the sweep | M6, M12, M15, M21 (domain half) — **done** | a command of the `#kind_*` shape over a type already in the environment |
| form declared, sweep written | M20, M21 (ambiguity half), M22 — **done** | `Relation.wellPosed`/`domain`/`ambiguity` censused by `#kind_wellposedness_coverage`; `@[kindDiagnostic]` censused by `#kind_diagnostic_coverage` |
| population must be *declared* first | M17, M18, M19, M26 (and D6, D7, D8, D13) | choose the declaration form — a bridge as data, a measurement, an adequacy claim, an uncertainty record — then the sweep is trivial |
| a proxy must be chosen | M7 | "relied on" has no mechanical meaning; someone decides the proxy and writes down that it is one |
| population lives downstream | D3 | the declared payload faces are a deployment repository's types (soil-moisture-workflows, sm-smap-nisar-lean), so the sweep is written there against PKC's receipt API, not here |

The deployment template's held rows are D4 (`#kind_discharges_decide`, a PKC command,
receipted), D5/D10/D16 (generators), and D12 — whose gate is the acceptance run itself, an
executable outside PKC. D12 cannot receipt through this extension and stays a declaration
site; a deployment document's matrix should say so rather than read it as walked.

And one item that is not a sweep at all, and comes first:

### Step 1 — make a green mean the census ran (done 2026-09-07)

Audits leave **receipts**. `PropertyKindCalculus.AuditReceipt` is an environment
extension; each instrumented command records `{ audit, scope, module }` when it completes
(for a `_clean` gate: when it *passes* — it throws otherwise, so a receipt is a pass).
`Conformance.status` then requires, for a rubric whose closure names audits, a receipt for
each named audit whose scope covers the document's `declScope`. A document that annotates
two theorems for a gated rubric and never runs the gate reads `partial`, not green.

Instrumented: `#kind_boundary_clean`, `#kind_mint_ratchet` (M9);
`#kind_dimensional_coverage`, `#kind_dimensional_clean`, `#kind_scc`, `#kind_scc_clean`
(M10); `#kind_contracts`, `#kind_contracts_decide` (M11 — the decide form also leaves the
report form's receipt, since it is the same sweep with kernel receipts); `#kind_relations`
(M13, M14); `#kind_discharges_decide` (D4); `#kind_examination_coverage`,
`#kind_examination_clean` (M6, step 2).

What it does not verify: that the named module contains the *right* audit over the *right*
namespaces beyond a prefix test, or that a pinned reading's pin is current. The receipt
says the command ran in this build over that scope. That is the fact a matrix can honestly
report, and it is a strictly stronger fact than "a site exists".

Scoping rule for the receipt check: an audit ran over namespaces `nss`; the document's
`declScope` is `ns`. The receipt covers the document iff some element of `nss` is a prefix
of `ns` or equal to it. A document with no `declScope` accepts any receipt for the audit,
which is right only for a document whose environment contains no other — the same caveat
`declScope` already carries.

### Step 2 — the M6 sweep (done 2026-09-07; SMM run done the same day, and it changed the sweep)

`#kind_examination_coverage ns …` (record) and `#kind_examination_clean ns …` (gate), in
`PropertyKindCalculus.ExaminationCoverage` (the `Dimension` library, beside
`DimensionalCoverage`, whose kind-resolution it reuses).

Population: every `DimensionedKind` declaration in scope whose dimension is one — decided
per generator by kernel reduction of the ℚ exponents, exactly as the dimensional-coverage
walk decides coherence. Not `KindOfProperty` constants: "dimension one" is a fact about the
`DimensionedKind`, the ISO 80000 catalogue declares most of its kinds inline inside one,
and a kind with no dimension declared is M10's finding, not M6's.

Predicate: the kind carries an examination principle (`examPrinciple ≠ none`).

Exception: `@[kindPrincipleFree "reason"]` on the `DimensionedKind` declaration. The
sweep lists such kinds as `⊘ exempted` with the reason; the gate does not fire on them.
This is the idiom the fresnel geometry kinds in soil-moisture-model need — today their
exemption is a docstring no audit can read.

Verdicts per row: `[individuated]` (has a principle), `⚠ UNINDIVIDUATED` (dimension one,
no principle, no mark), `⊘ exempted`, and — added after the soil-moisture-model run —
`⚠ UNDIMENSIONED`: a bare `KindOfProperty` constant in scope that no `DimensionedKind`
anywhere in the environment wraps. The report's last line counts each. The gate throws
while any row is `UNINDIVIDUATED` or `UNDIMENSIONED`, with the same no-message discipline
as the other `_clean` commands, and its error names the fixes and refuses the re-pin.

**What the SMM run found, and why the fourth verdict exists.** The first run read
`no dimension-one kinds in the given namespaces` over `SoilMoisture` — and the gate would
have passed. soil-moisture-model declares its kinds as bare `KindOfProperty` constants and
never a `DimensionedKind`, so the population as first defined was empty for exactly the
vocabulary M6 was written about, and a gate over an empty population is the vacuous
universal the test suite's charter warns against. The census now reports such kinds and the
gate fires on them: over `SoilMoisture` it reads **0 dimension-one kind(s); 96 kind(s)
UNDIMENSIONED**. The honest reading is that M6 cannot be decided for that model until it
declares dimensions (M10's obligation), and the matrix should say `partial` until then
rather than green. Row moved to `gate` on that basis.

### Step 3 — the remaining sweeps (done 2026-09-07 for M12, M15, M21's domain half)

Before writing, each of the six was explored against the types that exist. Three had a
population that is a type *and* a decidable predicate; three did not, and one was half of
each. The three are built; the rest are re-bucketed below with the reason and the form
they need.

`PropertyKindCalculus.ContractCoverage` (core, beside `KindIncidence`): three censuses over
declared boundaries (step 4's well-posedness census now makes it four), each a record
command, a `_clean` gate, receipts at the success points,
and a probe (`Tests.Core.ContractCoverage`) that pins every verdict class of every census,
the three gate refusals, three silent gates over a clean sub-namespace, and the two
attributes' own refusals.

* **M12 — `#kind_relation_coverage` / `#kind_relation_clean`.** Population:
  `contractSweepSubjects` (every `Provenance.Contract` in scope). Predicate: some
  `Provenance.Relation` names the contract on either side — relations walked *unscoped*,
  `@[kindCounterexample]` relations excluded, the join lifted out of `Index.Contracts` so the
  record table and the gate share it. Exception: `@[kindRelationFree "reason"]` on the
  contract. Validity of the edge stays `#kind_relations`' question (M13/M14).
* **M15 — `#kind_mereology_coverage` / `#kind_mereology_clean`.** Population: every produced
  port (`output` or `conditional`) of every contract in scope. Predicate: an `aggregations`
  entry for the port. No exception mark — the `AggregationClass` vocabulary is total over
  the honest negatives (`.intensive`, `.wholeProper`, `.countKeyed`, …), so "declares
  nothing" is exactly the finding.
* **M21, the domain half — `#kind_inversion_coverage` / `#kind_inversion_clean`.**
  Population: every contract in scope that is the *left* side of an `inverts` relation.
  Predicate: a `conditional` port (its decider reported where named). Exception:
  `@[kindInversionTotal "reason"]`. The ambiguity half ("surfaced rather than resolved to the
  first root") has no declaration form and is not walked; the row's closure says so.

**Run against soil-moisture-model (2026-09-07)**, by a scratch driver compiled in that
repository's own environment against its pinned PKC (the censuses copied in without receipts
or marks, which the pin lacks; `#kind_relations SoilMoisture` reads `3 theorem edge(s)`, none
violated, with the technical reference's imports):

| census | over `SoilMoisture` |
|---|---|
| M12 | 9 boundaries: 4 witnessed, **5 UNWITNESSED** — `rLutBoundary`, `retrieveForwardBoundary`, `avsFitLinearizedBoundary`, `rLutRetrievalBoundary`, `retrieveNewtonBoundary` |
| M15 | 52 produced ports: 5 classed, **47 UNDECLARED** (the five classed are the five `aggregations` clauses the repository has) |
| M21 | 2 inverted boundaries, **both UNGUARDED** — `solveSPD4Boundary`, `retrieveReflBoundary` (the R-LUT boundaries declare conditional ports with deciders, but no `inverts` edge names them) |
| M6 | 0 dimension-one kinds; **96 UNDIMENSIONED** (step 2) |

Each of these is what the model's matrix now owes: with the four rows at `gate`, its M6, M12,
M15 and M21 read `partial` until the gates run clean, which they will not until the
declarations are made or the exemptions written. That is the mechanism working, not a
regression. The pinned catalogue rows moved on the strength of the probe and the run, per §4.

**Re-bucketed to step 4, with the code facts that decided it** (the M20 and M21 bullets
record what decided the re-bucketing; step 4 has since resolved both — the fields exist):

* **M17.** The population exists — the `Carrier`/`NumCarrier` instance vocabulary, with
  `LawfulCarrier` as the line between specification and executable — but the predicate does
  not: a bridge is a `CarrierRefinement` instance only in PKC's own examples, while every
  bridge the worked instance names (`.magnitude` parity, tape parity, the rounded constant
  table) is a *theorem*, and `IEEE32Exec` is deliberately unbridged with the reason in a
  docstring. A sweep today would report every real bridge as missing.
* **M20.** The `inverts` edges are enumerable, but `Provenance.Relation` has no field for an
  existence-and-uniqueness witness or the domain it holds on, and `checkRelation` requires
  the `witness` to conclude with a round-trip `Eq` — so an `∃!` theorem such as the worked
  instance's `retrieval_well_posed` is structurally inadmissible where the edge lives.
* **M22.** Produced ports are enumerable, but nothing marks a port or a kind as a quality or
  conditioning output; the worked instance declares those as `IngestContract` slots and as
  kinds, not as ports of any `Provenance.Contract`. The population is not yet a type.
* **M21's ambiguity half.** No constructor, field or role anywhere expresses "more than one
  root, and said so"; `retrieval_ambiguous_of_collision` exists as a theorem hanging off no
  declaration.

### Step 4 — the declaration designs (M20 + M21's ambiguity half done 2026-09-07)

The design question is the declaration form; it is settled with the worked exception
idioms (steps 2 and 3) behind it, because a declaration that cannot express its own
honest negative will be written around.

**Done — M20 and M21's ambiguity half**, one form because they share the edge. Three
optional fields on `Provenance.Relation`: `wellPosed` (the declaration name of a
sorry-free theorem concluding with `∃!`), `domain` (the box or predicate the `wellPosed`
statement mentions — a witness with no domain is refused, because existence and
uniqueness are proved *on a declared domain*), and `ambiguity` (a sorry-free theorem
concluding with the negation of an `∃!` — non-uniqueness surfaced as a declaration).
`checkRelation` validates all three the way it validates `tolerance` and `hypotheses`,
on `inverts` edges only; `ExistsUnique` is matched by name, unresolved, so the
Mathlib-free core needs no import of the library that defines `∃!`. The census
`#kind_wellposedness_coverage` / `_clean` walks every `inverts` edge in scope:
`[well-posed]`, `[surfaced]`, `⊘` (counterexample), or `⚠ UNDECIDED`. No exception
mark — an inversion either has exactly one answer on a declared domain or it does not,
and either answer is a declaration, so the field pair is total over the honest negatives
(the M15 precedent). Two vocabularies met the census:

* the Water Cloud Model edge declares all three — `wcmForwardQ_well_posed` (`∃!` on
  `soilGainNonzero`, existence by `affine_surjective`, uniqueness by the round trip) and
  `wcmForwardQ_ambiguous_of_degenerate` (zero the gain and no backscatter has a unique
  preimage) — and `Audit.lean` pins the report `clean` with the gate's receipt, plus five
  falsification refusals (`∃`-shaped witness, missing domain, unmentioned domain,
  `Eq`-shaped ambiguity, well-posedness on a non-`inverts` edge);
* soil-moisture-model, by a population probe in its own environment: **2 inverts edges,
  both `UNDECIDED` by construction** — its pinned PKC's `Relation` has no such fields, so
  M20 cannot be declared there until the pin bumps. The theorems it would name already
  exist (`retrieval_well_posed_admissibleBox`, `retrieval_ambiguous_of_collision`); what
  was missing was only the field, which is exactly what step 3 found.

**Done — M22.** The form is `@[kindDiagnostic "what it diagnoses"]` on the
`KindOfProperty` — the kind-mark candidate, but with the census asking for a *produced
port* at the kind rather than crossing the boundary's harvested footprint: the predicate
then reads only declared contract values, and it does not under-report where the
diagnostic is computed by a sibling of the boundary's members rather than inside them.
Enrollment is the mark itself, so there is no exception mark (removing a mark is the
exemption), and `#kind_diagnostic_coverage` over a scope with no marks records that
visibly instead of passing in silence — the opt-in analogue of M6's `UNDIMENSIONED`
lesson. Two vocabularies met the census: the probe world (`[exported]` beside
`⚠ SIDECHANNELED` and the `UNREADABLE` boundary), and soil-moisture-model by a
population probe with the four `algorithm.qc.kinds` QC kinds standing in for the marks
its pin cannot yet declare — **all four (`avsFitRmse`, `nValid`, `condPivot`, `qcLevel`)
SIDECHANNELED over 9 readable boundaries**: they exist as kinds and as `IngestContract`
slots, and no declared boundary exports any of them as a port, which is M22's finding
stated mechanically.

**Open** — M17 from step 3; M18, M19, M26 in the model template; D6, D7, D8, D13 in the
deployment template. Each is `pending` because the thing to enumerate is a number in a
sentence, or a fact with no field to live in. The candidate the step 3 exploration
surfaced:

* **M17** — a bridge as data on the carrier's instance: `@[carrierBridge witness]` /
  `@[carrierBridgeFree "reason"]`, checked as `RelationLicense` rungs already are (a named
  theorem, existing, sorry-free), with an existing `CarrierRefinement` instance counting as
  the declared bridge already. The population question is harder than the sketch: the
  interesting carriers get `NumCarrier` through the parametric forwarding instance
  `instNumCarrierOfContext` (`torch/…/NumCarrierContext.lean` — "ℝ, binary32, and `Float`
  `NumCarrier`s with no new instance work"), so an instance-head walk enumerates none of
  them; the enumerable ground vocabulary sits at `Carrier`/`LawfulCarrier` (core) and
  `Context`/`MathCarrier` (torch) instances, split across libraries the core census cannot
  both see. What is settled: `CarrierRefinement` is the data-bearing bridge form
  (`QuantityRefinement.lean`), `Examples.MiniRefinement` its one instance the search
  found, and `IEEE32Exec`'s unbridged status is deliberate with its reason in
  `QuantityRefinement.lean`'s module docstring (a machine format's division needs the
  nonzero-denominator hypothesis, so its refinement is a theorem, not an instance) — the
  case `@[carrierBridgeFree]` exists to turn into data. The population definition comes
  first, settled against the torch library's own vocabulary.

### Step 5 — M7's proxy

Decide whether "every pair of dedicated kinds that meet in one operation signature" is the
claim, or something narrower. Write the decision into the rubric's `closure` string as a
proxy, not as the obligation.

## 4. The rule for calling a step done

A sweep exists when three things are true, in this order:

1. its test probe pins **every verdict class**, including the exempted one, so the
   classifier is exercised rather than the happy path;
2. it has been run against soil-moisture-model's kinds — the only worked instance — and
   the result read, because a sweep that has never met a real vocabulary is a hypothesis
   about one;
3. the rubric's `closure` in the catalogue names it, and `rubric_closure_count` has moved.

Step 3 is the one that changes the published number; steps 1 and 2 are what make the
number true.

## 5. Progress log

* **2026-09-07** — Framing fixed: `Closure` and `obligation` added to `Rubric` for all 43
  rubrics; both blueprint chapters restructured (template table before the argument; a
  closure section with the generated profile). Step 1: `AuditReceipt` extension; nine
  existing commands instrumented; `Conformance.status` receipt-aware; probe
  `Tests.Core.Rubrics` extended with a gated rubric that flips from `partial` to `proved`
  when the audit runs. Step 2: `ExaminationCoverage` with `@[kindPrincipleFree]`; probe
  `Tests.Dimension.ExaminationCoverage` pins all three verdict classes, the gate's refusal,
  a clean sub-namespace with an exemption (which must not fire the gate), a stale mark on
  an individuated kind (the principle wins), and the attribute's two refusals. Every
  existing pinned probe of the nine instrumented commands is unchanged by the receipts
  (1758-job build, green on the first run after two compile fixes).
* **2026-09-07** — The first real vocabulary, all of it. Both censuses run over every part of
  ISO 80000 and pinned as records in `tests/PropertyKindCalculus/Tests/Iso80000/Coverage.lean`
  (the `Tests.Iso80000` tier; the pins are the compiler's output, generated by a driver, not
  typed). Where the audits for the ISO parts *run*, from now on, is that file, and
  `scripts/gen-iso80000-coverage.py` is how it is regenerated after a catalogue change:
  it runs the censuses, writes the records verbatim, pins a gate only where a report says
  `clean`, and derives the header's summary from the records (no dates, no typed counts —
  an unchanged catalogue regenerates byte-identically; `--check` diffs without writing).
  * `#kind_examination_coverage`: **Part 11 is clean — 115 characteristic numbers, 115
    individuated**, each by its `context: defining-ratio` principle. Every other dimension-one
    kind in the catalogue is `UNINDIVIDUATED`: 169 across Parts 3–10, 12 and 13 (the angles,
    the optical ratios, the drag and friction coefficients, the CIE coordinates …); Parts 1
    and 2 declare none. The catalogue attaches principles to its length species (dimension
    `L`), where M6 does not ask for them. Gates pinned over Parts 1, 2 and 11 only.
    Why: every one of the 169 is built by `dimKind id Dim.one`, which writes no
    principle; Part 11's `charNum` is the only constructor that does. The standard's own
    definitions are principles in exactly Part 11's sense — 5-25.1 defines efficiency as
    the quotient of work delivered by a heat engine and heat supplied, `η = W/Q` — so a
    `charNum`-shaped constructor per part would individuate them mechanically. The text
    must come from the standard, item by item (not from memory: AI-POLICY §2.1 applies to
    a defining ratio as much as to a citation), and where two items share a ratio the
    context prefix is what keeps them distinct, as Part 11's clauses do.
  * `#kind_dimensional_coverage`: twelve parts coherent, gated. **Part 5 records one
    `CONFLICTING` edge**: `Part5.entropy` (`dimKind "entropy" TDim.heatCapacity`) and
    `Part13.entropy` (`dimKind "entropy" Dim.one`) build the same `KindOfProperty` value, so
    thermodynamic and information-theoretic entropy are *one kind* with two dimensions. Only
    the dimension functor sees it, and only with the whole catalogue in one import closure —
    which is why the probe imports the root. Not gated; awaiting a maintainer's decision
    (a distinct `id`, or a principle on the Part 13 kind, which is also one of its
    `UNINDIVIDUATED` rows).
  * Reader fix on the way: Part 11's principles are built by `++`, which `whnf` never reduces
    to a literal, so the first run printed `?` for all 115 — the census reporting its reader.
    `strOf` now falls back to the evaluator (the `evalRelation` idiom); the probe pins the
    full principles.
  **Not yet run against soil-moisture-model** — that repo pins PKC from git.
* **2026-09-07** — Step 3. `PropertyKindCalculus.ContractCoverage`: `#kind_relation_coverage`
  / `_clean` (M12, exception `@[kindRelationFree]`), `#kind_mereology_coverage` / `_clean`
  (M15, no mark), `#kind_inversion_coverage` / `_clean` (M21 domain half, exception
  `@[kindInversionTotal]`); the witness join lifted from `Index.Contracts` so table and gate
  share it; probe `Tests.Core.ContractCoverage` pins every verdict class (including
  `UNREADABLE` for a contract at non-`String` node types, the counterexample rows, and both
  stale-mark cases); eight annotation-index entries. Run against soil-moisture-model by a
  scratch driver in that repository's environment: M12 5/9 unwitnessed, M15 47/52 undeclared,
  M21 2/2 unguarded (table in step 3). Catalogue: M12, M15, M21 → `gate`; M17, M20, M22
  `pending` strings now say which declaration form is missing. `Tests.Core.Rubrics`' site-
  discharged example moved from M12 (now gated) to M17.
* **2026-09-07** — Step 2's SMM run, and the defect it found. `#kind_examination_coverage
  SoilMoisture` read `no dimension-one kinds` over a vocabulary of 96 `KindOfProperty`
  constants — none wrapped by a `DimensionedKind`, so none in the population, so the gate
  would have passed vacuously. Fourth verdict `⚠ UNDIMENSIONED` added (a bare kind in scope
  no `DimensionedKind` in the environment wraps; the gate fires on it); probe extended with a
  bare kind and a wrapped one; ISO pins unchanged under the new census
  (`gen-iso80000-coverage.py --check`: up to date — the catalogue declares no bare kind).
  SMM now reads `0 dimension-one kind(s); 96 kind(s) UNDIMENSIONED`. M6 → `gate`.
* **2026-09-07** — `scripts/gen-iso80000-coverage.py` committed as the generator of
  `Tests.Iso80000.Coverage`: regeneration is byte-identical on an unchanged catalogue, and
  `--check` diffs without writing.
* **2026-09-07** — Step 4, first form: M20 and M21's ambiguity half. `Provenance.Relation`
  gains `wellPosed`/`domain`/`ambiguity`; `checkRelation` validates them (shape by the
  unresolved name `ExistsUnique`, so the Mathlib-free core states nothing it cannot
  import); `#kind_wellposedness_coverage` / `_clean` census them; the survey and the index
  `relations` table render them as clauses. Probes: `Tests.Core.ContractCoverage` pins all
  four verdict classes (the `[surfaced]`-only collapsing forward included) and the gate's
  refusal; the Water Cloud Model edge declares all three fields with real theorems and five
  falsification probes pin `checkRelation`'s refusals; `Audit.lean` pins the census `clean`
  over `UncertaintyExamples` with the gate's receipt. SMM population probe: 2 inverts
  edges, both UNDECIDED until its PKC pin carries the fields. Catalogue: M20 → `gate`;
  M21's closure names both gates (`kind_inversion_clean`, `kind_wellposedness_clean`), so a
  conforming document must run both.
* **2026-09-07** — Step 4, second form: M22. `@[kindDiagnostic "what it diagnoses"]` on a
  `KindOfProperty`; `#kind_diagnostic_coverage` / `_clean` ask the declared boundaries in
  scope for a produced port at each marked kind (declared values only — no harvest, so a
  diagnostic computed beside the boundary's members is still owed a port). Probe pins
  `[exported]`, `⚠ SIDECHANNELED`, the `UNREADABLE` boundary, the gate's refusal, a clean
  sub-namespace, and the attribute's two refusals. SMM population probe: the four QC kinds
  all SIDECHANNELED over 9 readable boundaries (table in step 4). Catalogue: M22 → `gate`.

## 6. The port-vocabulary migration — `Provenance String String` → `Provenance NodeId KindRef`

> Status: **approved 2026-09-07 (types, scoping doctrine, and decisions A–D below); phases
> P1–P6 open.** Acceptance tests landed first, at
> `tests/PropertyKindCalculus/Tests/Core/PortNameScopes.lean`: the module header carries the
> analysis this section acts on, and its pinned probes are the record every phase answers to.
> Audience: PKC maintainers; downstream authors of `Provenance.Contract` values (SMM, SMW),
> who migrate at their own pin bump.

### 6.1 The finding

`Provenance ν κ` is parametric in name only: `contractValueOf` demands
`Contract String String` by defeq, so every census reads exactly one instantiation and any
other is `⚠ UNREADABLE`. Both `String`s flatten a grammar:

* **κ (kinds)** is a *rendering* — `Meta.ppExpr` relative to the defining module's namespace
  when closed, a binder name when not. Four consequences, each pinned in `PortNameScopes`:
  `Contract.standsFor` compares kinds byte-exact, so `#kind_contracts` must replay each
  contract under its defining module's `currNamespace` and inherit its `open`s; a suffix
  tolerance (`pk == long || long.endsWith ("." ++ pk) || …`) duplicated at four sites lets
  one port kind match two declarations — the M22 census reads `clean` on a double-match it
  should refuse; a signature kind is a `" → "`-joined string re-parsed by `splitOn`; and the
  port kind is the only declaration-referencing `Contract` field never resolved against the
  environment (deciders, aggregations, suppliers, tolerances all are).
* **ν (nodes)** is a flattened five-class sum — signature binder, body-`let` name,
  `result`/`result.N` structural, config constant full name, `_N` gensym — with level
  (`step/`), instance (`#k`), and field-path (`.lo.q`) punctuation on top. A string cannot
  say which class it is in: a binder named `result` collides with the output node; an
  arrow-form signature ports at a hygiene name; shadowed binders and lets corrupt wiring
  silently; an erased inline compound degrades to `exit instHAdd.hAdd`; a member's nodes are
  all renamed when it gains a second call-site instance (`member/x` → `member#1/x`), so a
  true contract breaks under a semantic no-op.

### 6.2 The vocabulary (prelude-only; `Lean.Name` is prelude-level)

```lean
inductive KindRef
  | decl (n : Lean.Name)              -- a kind declaration, by environment name
  | param (binder : String)           -- a generic kind binder of the member's signature
  | sig (components : List KindRef)   -- the module-valued port form (the " → " join)
  | tuple (components : List KindRef) -- a carrier with several kind parameters (the ", " join)
  | unkinded                          -- a config constant porting no kind (the "_" sentinel)
  | opaque (rendered : String)        -- anything else; byte-exact comparison, contract-refused

inductive NodeRef
  | binder (name : String)            -- signature telescope
  | letBound (name : String)          -- body-named: `let`s and single-alt matcher binders
  | result (component : Option Nat)   -- structural; 1-based, matching the rendering
  | const (n : Lean.Name)             -- a config port, or a constant-named operand
  | fresh (n : Nat)                   -- gensym: graph-internal, contract-refused
  | unresolved (rendered : String)    -- operand degradations, visible, contract-refused

inductive Level
  | inst (member : Lean.Name) (ordinal : Nat)  -- one call-site instance, ordinal 1-based
  | member (member : Lean.Name)                -- member-scoped: shared config ports

structure NodeId where
  level : Option Level := none
  root  : NodeRef
  path  : List String := []           -- field projections, segments unjoined
```

`tuple`, `unkinded`, and `unresolved` are forced by the survey, not chosen: `carrierKind?`
joins a multi-kind carrier with `", "`; a slotless config constant ports at kind `"_"`; and
`refName`'s fallbacks can name an operand `"_"` or a literal in an already-ill-formed graph,
where the verdict must still be able to say so. `Level` is a sum because config ports are
member-scoped, not instance-scoped — one address, however many instances. `Level.inst`
always carries the ordinal; only the rendering collapses `#1` when the member has one
instance, which is what makes node identity stable under call-site growth.

Scoping doctrine (settled): `.binder`/`.param` resolve in the member's signature telescope;
`.letBound` in the body's kind-bearing names — `let` binders and single-alternative matcher
binders, one flat namespace, no nesting path (path-as-identity rejected: unstable under
hoisting, identical for same-depth shadowing, matcher-generated through match arms). All
names checked accessible (no hygiene) and unique in scope, refused otherwise; an erasure
victim that is not a nameable node is refused ("let-bind the value you erase"); gensyms and
`unresolved` stay graph-internal — a contract mentioning either is refused. Renaming a
binder or let a contract names is an interface change by doctrine.

### 6.3 What the structure deletes

The suffix tolerance at its four sites; `#kind_contracts`' `currNamespace` replay and its
`open`s requirement; `splitOn " → "` in `checkSuppliers`; `levelOfNode`'s `splitOn "/"` and
`memberOf`'s `splitOn "#"`; the `result` reserved word and its collision; the config
full-name `startsWith` matching; and the whole-string monomorphization map — kind
substitution becomes structural on `KindRef`, recursing into `sig`/`tuple` components,
which whole-string replacement never could.

### 6.4 Decisions (settled 2026-09-07)

* **A — the other declaration references migrate in the same break**: `Contract.members`,
  `deciders`/`suppliers` payloads, the `AggregationClass` tolerance/condition/sortal/
  transport/law names, and `Relation`'s tolerance/hypotheses become `Lean.Name`. One API
  break, not two.
* **B — stays `String`**: `Contract.name` (a title), `Occurrence.site` (attribution, not
  identity), `attested` reasons, the unkinded red inventory.
* **C — `EdgeFamily.step` carries `(member : Lean.Name) (inst? : Option Nat)`**: the step
  label participates in call grouping at assembly, so it is identity, not rendering.
* **D — the last-component collision refusal at assembly stays**: it is rendering-only now,
  and it keeps figures unambiguous.

Rendering policy: renderers print the structured values in the established formats —
`level/root.path`, `member#k` only when the assembly has more than one instance, `.decl` by
last component, `sig` with `" → "` — so pins survive except where `ppExpr` emitted partial
qualification, and those churn once and never depend on context again. Gates no longer ride
on renderings. Authoring: kind and member positions are written as double-backtick name
literals — resolved and checked at elaboration, so namespaces and `open`s work at
authoring time while identity stays absolute — and nodes through smart constructors; a printer
command emits a computed boundary as paste-able constructor syntax, which is how the
existing 88 authoring sites across 22 files migrate mechanically.

### 6.5 The phases

Each phase ends green for every build target it touches, committed and pushed. P3 is the
break: from P3 until P5 the roots not yet migrated are red, and each commit message says
which.

| phase | scope | gate |
|---|---|---|
| P1 | this plan section | doc only — **done 2026-09-07** |
| P2 | the four inductives + `ToExpr`/`BEq`/`DecidableEq` instances + type-level probes, additive | `lake build` + new probes green |
| P3 | the harvest and assembly (`KindIncidence`), checkers, `contractValueOf`, refusals; `Provenance.lean` breaking edits (A, C); core-library consumers (`ContractCoverage`, `KindLedger`, `KindGraphD2`, `ModuleCard`, `KindQueries`, `Influence` instantiations) | `lake build` (default target) green |
| P4 | secondary roots: `graph/` (Footprint), `index/` (Contracts), remaining srcDir roots | their targets green |
| P5 | `tests/` migrated and re-pinned; the `PortNameScopes` pins flip to the designed refusals and cleanups | `lake build Tests` green; doc-pins gate |
| P6 | `ForPhysLib/` + `examples/` migrated and re-pinned; document mentions updated; final sweep | all targets + `scripts/check-doc-pins.py` green |

The acceptance flips P5 answers for: `resultShadow` becomes clean (`.binder "result"` is not
`.result`); `arrowForm`, `shadowedBinders`, `twoParams`, `erasesInline`/`erasesTwoInline`,
`shadowLets`, and `matchArms` become refusals; `seqLets`/`nestedValue` stay green; the M22
census pin flips to `SIDECHANNELED` for the kind the suffix tolerance used to credit.

### 6.6 Progress log

* **2026-09-07** — Plan approved (types, doctrine, A–D); this section written; P1 done.
